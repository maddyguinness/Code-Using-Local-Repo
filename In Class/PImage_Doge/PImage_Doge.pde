PImage doge; //P stands for processing.... pixel, width, height



//loading an image onto the canvas

void setup() {
  //image = loadImage();
  //image(image name ,0,0, width, image.height);
  //imageMode(CENTER);
}
//
void draw() {
  //rotate or whatever
  //image(image,0,0)
  
}